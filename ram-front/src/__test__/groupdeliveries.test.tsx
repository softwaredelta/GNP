import "@testing-library/jest-dom/extend-expect";
import { render } from "@testing-library/react";
import ManagerDeliveryCard from "../components/generics/""
import GroupDeliveries from "../pages/GroupDeliveries"

describe("Group deliveies list", () => {
    it("renders all the group de", () => {
        render(
            <ManagerDeliveryCard/>
        );
        
    })
})